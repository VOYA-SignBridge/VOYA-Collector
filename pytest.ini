[pytest]
testpaths = tests
pythonpath = backend
norecursedirs = node_modules .venv .venv_py313_backup _tmp signbridge-prod frontend
addopts = -ra
markers =
    requires_db: needs a reachable PostgreSQL (skipped automatically when the DB is not reachable from the host)
